# 服务端渲染

```markdown
# 补充
1. 当一行代码以 （
                 [
                 `
    为开头时，需要使用; 作为前缀, 用于避免一些语法解析错误
    
2. 模板字符串，反引号, 支持换行, 写什么就是什么             

3. 除了Content-Type可以用来指定编码，也可以在HTML页面中通过meta元数据来声明当前文本的编码格式，浏览器也会识别它

4. 二进制数据转字符串 data.toString()

5. node也可以使用模板引擎

6. npm 该命令在哪执行就会把包下载到哪里。默认就会下载到node_modules目录中
    node_modules 不要改，也不支持改
    
7. 模板引擎的循环 {{each hobbies}} {{$value}} {{/each}}
    模板引擎最早就是诞生于服务器领域，后来才发展到了前端
    
8.  模板引擎的render方法需要接受字符串作为参数
       art-template
    
    // data为二进制数据
    服务端渲染 template.render(data.toString(), {
        name: 'Jack'
    })    
```



```node
客户端渲染不利于SEO搜索引擎优化
服务器渲染是可以被爬虫抓取到的，客户端异步渲染是很难被爬虫抓取到的
真正的网站就是客户端渲染，也是服务端渲染
商品列表采用的及时服务端渲染，目的为了SEO搜索SEO搜索引擎优化
评论列表为了用户体验，不需要seo优化，采用的是客户端渲染
```



